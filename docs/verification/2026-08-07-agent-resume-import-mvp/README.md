# Agent 简历导入 MVP 验收记录

验收日期：2026-08-07

## 结论

本地 Test 候选通过。在线简历库与完整简历库保持独立；外部 Agent 可以只导入选中的在线简历，也可以把完整简历直接导入现有简历库。智聘不反向控制 Agent，也没有新增自动关联、自动移动或自动删除。

## 真实接口证明

使用 Flask 真实路由和临时数据库执行 6 个组合场景，结果为 `6 passed`：

- 招聘专员导入并读取自己的在线简历。
- Agent 再次同步时保留招聘专员人工修改，并合并完整聊天记录。
- 在线简历硬删除后，已统计的导入数量不减少。
- 月度看板只统计本人、当月的在线简历导入数量。
- 关闭简历 AI 后，Agent 仍可把结构化完整简历和原始文件直接导入现有简历库。
- 招聘专员替换完整简历后，同一外部导入编号仍不会重复创建候选人。

完整简历导入还验证了：加入招聘需求失败时不留下候选人、空批次或成功收据；多文件中已成功的项目不受后续失败影响。

## 自动检查

- 后端全量：`764 passed`。
- 前端契约：`178 passed`。
- 前端类型检查、代码规范、正式构建：通过。
- 前端包体积：通过。
- 前端与 Python 依赖安全：通过，无已知漏洞。
- 数据库迁移头：`20260807_16 (head)`。
- Git 差异格式与本地发布门禁：通过。

## 浏览器验收

使用本地真实后端、登录桥和 Chromium 执行招聘专员流程，结果为 `2 passed`：

- 招聘专员能登录并进入自己的真实工作入口。
- 招聘专员能看到并打开“在线简历”页面，页面没有加载失败。

验收结束后已关闭临时后端、登录桥和前端服务。

## 边界

- 本记录不含真实候选人信息、密码、Token 或聊天内容。
- 未提供公司 `SIT_ENV_FILE`，因此只证明本地 Test/SIT 候选门禁通过，不冒充公司 SIT 现场验收。
- 本次未推送远端、未部署。
