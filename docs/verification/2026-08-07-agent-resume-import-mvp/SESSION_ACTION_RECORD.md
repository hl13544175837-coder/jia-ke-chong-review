# 当前会话行动记录

1. 排查 BOSS 直聘反复提示“账号已经登录”：确认根因是多个自动化浏览器会话抢同一登录状态，收口为单窗口/单任务空间，并保留人工扫码控制权。
2. 固化 MVP 产品路线：在线简历库与完整简历库独立；在线简历保留完整聊天并可编辑、删除；完整简历保留原件和可编辑结构化信息，两边不自动搬移、覆盖或删除。
3. 完成 Agent 单向导入：支持选择性导入在线简历，也支持把候选人发送的完整简历直接导入现有简历库并进入具体招聘需求。
4. 补齐安全和防重：Agent 使用 30 天限权凭证；不能访问普通业务接口；人工修改优先；聊天合并；删除后不重复恢复；完整简历按外部编号防重，失败不留候选人、空批次或假收据。
5. 补齐产品页面和统计：新增招聘专员“在线简历”入口、列表、完整聊天、编辑、删除和客观导入数量，不做排名或自动绩效判断。
6. 用 Ego 外部进程真实核验：在线简历成功创建；完整 PDF 成功入库并进入指定 Demand 的待筛选阶段；原件、结构化内容和导入收据均可反查。
7. 修复接入技术债：新增“连接 Agent”，选择具体招聘需求和 BOSS 账号后，一键生成限权凭证与完整提示词；凭证关闭后清空，不写入浏览器存储或日志。
8. 完成后端、前端、浏览器、依赖安全、数据库迁移和发布门禁检查；最终远端状态以本次推送后的 `cfpd/test` SHA 为准。

