# 历史试用环境快照（需现场复核）

> 原文曾叫“当前试用环境交接说明”。现在统一改成历史快照。
> 这份文档只说明某一次本机试用环境曾经怎么接，不等于打开新对话时的当前真实状态。

## 先说红线

不要只凭本文回答“现在已经接上公司 MySQL”“现在同事可以访问”。这些话必须先现场复核。

只有同时确认下面几件事，才能把历史快照升级成当前结论：

```bash
pwd
git remote -v
test -f backend/.env && echo "backend/.env exists" || echo "NO backend/.env"
python3 backend/scripts/check_pilot_readiness.py
```

如果 `backend/.env` 不存在，或自检没有跑通，就只能说：**仓库里有历史试用快照，但当前环境未确认**。

启动服务后，再按实际地址验证：

```bash
curl -I http://127.0.0.1:5000
curl -I http://<当前本机局域网IP>:5000
```

## 历史上曾经接上了什么

当时的工作区不要只看固定目录名。先用 `pwd` 和 `git remote -v` 确认：只要当前目录是智聘仓库，并且 `origin` 指向下面的 CFPD GitLab 地址，才把它当作公司 GitLab 工作副本；本机目录名可能是 `/Users/yenns/Desktop/智聘`、`/Users/yenns/Desktop/zhipin-deploy-gitlab` 或其他。

当时记录的公司 GitLab 远程仓库是：

```text
git@git.ymdd.tech:cfpd/zhipin-mvp.git
```

当时不是“命令行单独连了一下数据库”，而是项目后端通过 SQLAlchemy 连接 MySQL，并且接口验证通过。

当时连接的是公司 MySQL 测试库：

```text
地址：10.202.30.43
端口：3306
库名：test_zhipin
账号：test_zhipin
MySQL 版本：8.0.32
```

数据库密码不要写入本文档。真实连接串在本机忽略文件：

```text
backend/.env
```

## 历史本机试用地址

当时后端以 Flask 单进程方式跑在 5000 端口，并托管前端构建产物。

本机访问：

```text
http://127.0.0.1:5000
```

当时局域网给同事试用：

```text
http://192.168.80.117:5000
```

如果换了 Wi-Fi、换了网络、电脑 IP 变了，或者当前没有 `backend/.env`，这个地址就不能继续当真。必须重新查本机局域网 IP，并同步更新 `backend/.env` 里的 `CORS_ORIGINS`。

## 历史 MySQL 里有什么数据

当时已经用项目启动自动建表，并执行过：

```bash
cd backend
python seed_dev.py
```

当时测试库里已有试用数据：

```text
用户：7
岗位：4
候选人：10
流程记录：36
```

试用账号：

```text
管理员：admin01@mvp.local
招聘经理：manager01@mvp.local
招聘负责人：lead01@mvp.local
招聘专员：hr01@mvp.local
招聘专员：hr02@mvp.local
招聘专员：hr03@mvp.local
面试官：interviewer01@mvp.local

统一密码：Zhipin2026
```

这些是演示/试用账号，不是正式生产账号。

## 已经为 MySQL 做过哪些代码和文档改动

已做的关键改动：

- `backend/requirements.txt` 增加 `PyMySQL==1.1.2`
- `backend/app/config.py` 支持 `mysql://` 自动转成 `mysql+pymysql://`
- `backend/scripts/check_pilot_readiness.py` 允许 MySQL 作为试点/生产数据库
- `backend/scripts/backup_pilot_data.py` 支持 MySQL 备份 dry-run 和 `mysqldump`
- `backend/.env.example`、`README.md`、`RUNNING.md`、`DEPLOYMENT.md`、试点检查清单已同步 MySQL 说明

当时 `backend/.env` 的核心配置方向是：

```env
FLASK_DEBUG=false
DATABASE_URL=mysql+pymysql://...
ALLOW_PUBLIC_REGISTRATION=false
SECURITY_HEADERS_ENABLED=true
RATE_LIMIT_ENABLED=true
BACKUP_DIR=<当前仓库或服务器备份目录>/backups
AI_RECRUITMENT_COMPLIANCE_ACK=true
CANDIDATE_PRIVACY_NOTICE_URL=https://zhipin.内网域名/privacy
AI_HUMAN_REVIEW_REQUIRED=true
```

## 当时验证过什么

当时验证通过：

```bash
python3 backend/scripts/check_pilot_readiness.py
```

当时结果：试点部署前自检通过。当前必须重新运行，不能沿用这个结果。

当时验证 MySQL 连接状态：

```text
driver=mysql+pymysql
host=10.202.30.43
database=test_zhipin
```

当时接口验证通过：

```text
/                 200
/api/auth/me      200
/api/jobs         200
/api/candidates   200
/api/bi/overview  200
```

当时前端构建通过：

```bash
cd frontend
npm run build
```

当时 MySQL 相关测试通过：

```bash
python3 -m pytest backend/tests/test_pilot_hardening.py backend/tests/test_deployment_artifacts.py -q
```

## 仍然要注意什么

即使现场复核后确认仍能访问，也只能说“可以给内部同事临时试用”，不能直接说是正式生产。

原因：

- 当前库名是 `test_zhipin`，看名字就是测试库
- 数据库密码已经在聊天里出现过，正式上线前必须换正式账号密码
- 当前账号是演示账号，密码统一，不能作为正式员工账号
- 还没有完成生产级 HTTPS、正式域名、正式备份恢复演练、监控告警、真实数据权限审计
- 全量后端测试结论以当前仓库实际运行结果为准；不要沿用旧分支里 BOSS QR / stoken 测试失败的历史结论。

## 下次 Codex 应该怎么回答

如果用户问“现在连上了吗”：

先跑本文开头的现场复核命令。没跑通之前，不要直接说已接入。正确回答是：历史快照显示曾接入公司 MySQL 测试库 `test_zhipin`，但当前是否仍接入，要以 `backend/.env` 和自检结果为准。

如果用户问“能不能给别人用”：

先确认服务正在 5000 端口运行、当前局域网 IP 正确、`CORS_ORIGINS` 已同步、登录和核心接口能通。没确认前，不要复用历史的 `http://192.168.80.117:5000`。

如果用户要继续推进正式上线：

优先处理：

1. 换正式数据库账号密码，不再使用聊天里暴露过的测试密码。
2. 确认正式域名和 HTTPS。
3. 更新 `CORS_ORIGINS` 为正式域名。
4. 做一次真实 `mysqldump` 备份和恢复演练。
5. 用招聘专员、招聘经理/负责人、面试官、管理员四类角色走一遍关键路径。
