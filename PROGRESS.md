# 招聘需求工作台优化进度

- 目标：让状态、排序、搜索和筛选的文字与实际结果完全一致。
- 顺序：先锁定失败测试，再修状态与排序，再做筛选和文案，最后实机与全量验收。
- 约束：只修改任务书白名单，保留真实 API、需求抽屉和“选候选人”流程。
- 基线：分支 `codex/local-original-recruitment-flow`，HEAD `40bddb2`。
- 基线：相关测试 4/4，lint 与 type-check 通过。
- 最大风险：`status=pending` 与 `approval_status` 混用，以及筛选选项从已筛结果生成。
- 红灯证据：`node --test frontend/tests/demand_workspace_ux.test.mjs` 失败 1、通过 0；失败原因是旧页面没有独立工作台规则文件。
- 实机补充红灯：未选择阶段却出现“阶段：全部”；新增检查后失败 1、通过 0，已进入修复。
- 语义补充红灯：驳回需求仍可能显示“需求待确认”；新增检查后失败 1、通过 0，已进入修复。
- 状态：顶部入口最终精简为全部、待审核、招聘中、已完成、已停止；行内仍保留已驳回、已暂停、已关闭、已取消的准确状态。
- 排序：最新发布、优先级、截止日期使用同一入口，当前方向明确显示；空日期固定排末尾。
- 搜索筛选：五类搜索字段、四类筛选、单个删除与全部清空已接入，选项来自完整需求集合。
- 实机：七个标签逐个选中；五类搜索结果分别为 1/1/3/1/1；组合部门+城市后 1 条，单删与清空后恢复 3 条。
- 实机：排序入口可在优先级降序/升序、截止日期升序、最新发布降序间切换。
- 实机：Java 需求详情正常打开；“选候选人”在同页打开正确需求工作区并显示 13 位真实候选人。
- 窄屏：820×900 下搜索、筛选、排序同排且不重叠，表格使用横向滚动。
- 全量第 1 轮：125 条中 124 通过，旧导航契约要求内部名称“需求审核”；采用内部兼容名+用户显示名“招聘需求”修复，未修改旧测试。
- 全量第 2 轮：125/125 通过，失败 0、跳过 0；lint、type-check、正式 build 全部退出码 0。
- 仓库检查：`git diff --check` 退出码 0；白名单外仅保留任务开始前已有的后端、演示脚本和 `.superpowers/` 改动。
- 实机补充：选择“上海”后切换到“招聘中”，城市筛选标签和 1 条结果均保留；切换状态不会偷清筛选。
- 状态精简红灯：新规则要求暂停、关闭、取消统一归入“已停止”，旧实现测试失败 1、通过 0。
- 状态精简绿灯：目标测试及两个导航契约测试 3/3 通过；暂停、关闭、取消均归入“已停止”。
- 精简后全量：前端测试 125/125 通过，失败 0、跳过 0；lint、type-check、build、`git diff --check` 均通过。
- 精简后实机：顶部仅显示 5 个状态入口；已驳回、已暂停、已结束和“更多”均不存在；点击“已停止”可正常选中并筛选。
