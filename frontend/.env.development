# 本地开发（npm run dev）直接跨域访问测试网关。
# 仅 dev 模式加载；生产构建用 frontend/Dockerfile 的 build-arg，两者互不影响。
# 前提：测试网关需允许 http://localhost:5173 的 CORS（否则浏览器跨域会被拦）。
VITE_API_BASE_URL=https://test-pgsgw.yimidida.com/zhipin-server/api
VITE_OAUTH_BASE_URL=https://test-pgsgw.yimidida.com/pgs/oauth
