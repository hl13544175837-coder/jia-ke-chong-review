# 前端环境变量模板（Vite）。复制为 .env / .env.local 供本地使用。
# 注意：根目录 .dockerignore 排除了 .env*，这些文件不会进 Docker 构建；
# 容器构建的网关地址由 frontend/Dockerfile 的 ARG VITE_API_BASE_URL 注入。

# API 请求前缀（所有接口的统一前缀，见 src/lib/apiBase.ts）。
# 留空 / 不设 → 默认 '/api'，由 Vite 代理转发到本地后端（见 vite.config.ts）。
# 需要本地直连网关联调时，改成对应网关地址：
#   测试网关：https://test-pgsgw.yimidida.com/zhipin-server/api
#   生产网关：https://pgsgw.yimidida.com/zhipin-server/api
# VITE_API_BASE_URL=/api
