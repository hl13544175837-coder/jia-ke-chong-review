# 面试闭环分支总结

- 已补齐面试通知投递/重试、面试官接单或拒绝、短期签名链接免登录反馈、HR 查看结果并继续 Offer/入职的闭环；反馈不自动推进主流程。
- 本地使用 `compose.local.yaml` 全容器运行：MySQL 8.0.32 `13306`、后端 `15001`、前端 `15173`、通知模拟器 `19090`，全部只绑定 `127.0.0.1`。
- 浏览器已验收：创建 1 个岗位和 1 个 Demand，导入并修改候选人，加入流程，安排面试，通知送达，免登录接单/评分，HR 查看结果，Offer 接受、入职并完成 Demand。浏览器扩展未获文件 URL 权限时，上传步骤改用同一 `/api/resume/upload` 接口提交 DOCX，随后仍在页面核验入库结果。
- 数据终态：岗位 1、候选人 1、Demand `filled`、流程 `completed`、阶段 `onboarded`、面试任务 `completed/accepted`、反馈 5 分且通过、Offer `accepted`。
- 验证：后端完整套件 443 通过；本轮部署契约与面试用例 43 通过；前端完整契约、类型检查、Lint、构建与依赖审计通过；最终容器全部健康。
- 已同步 README、运行/部署说明、PRD、SDD、试点/上线清单和环境变量模板。非阻塞的候选人编辑表单可访问性问题保留在 Issue #7。
