# 招聘闭环迁移到目标态第一阶段

- 基线：`origin/codex/target-mode-stage1-modules`（`42811e3`）。
- 任务：GitHub Issue #15，交付 PR #18；基线缺陷 #16、演示数据清理 #17 已关闭。
- 结果：保留甲方招聘中控台、模块 facade 和 integrations capability 边界，完成岗位、需求、DOCX 入库、资料修改、流程推进、安排面试、企微免登录评分评价及 HR 结束面试闭环。
- 运行：MySQL 8.0.32、前后端和企微替身均在 Docker 中运行，宿主机仅使用高位端口并绑定 `127.0.0.1`。
- 认证：经项目负责人授权迁入已评审的 `local/gateway` 模式分流；本地镜像不会请求 PGS，SIT/生产默认保持 `gateway`，不改变后端 JWT/RBAC 规则。
- 验收：前端契约、类型检查、Lint、生产构建、真实浏览器登录、314 项后端业务测试及 49 项配置/加固测试通过。
- 已知范围外演示模块：Offer 管理仍使用 `MOCK_OFFERS`，人才地图仍为前端本地状态；两者不参与本轮“HR 结束面试”闭环。
